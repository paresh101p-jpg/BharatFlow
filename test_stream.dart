import 'package:supabase_flutter/supabase_flutter.dart'; void main() { final client = SupabaseClient('url', 'key'); client.from('table').stream(primaryKey: ['id']).inFilter('status', ['Replied', 'Approved']); }
