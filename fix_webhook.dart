import 'package:supabase/supabase.dart'; import 'lib/core/constants/api_keys.dart'; import 'dart:io'; void main() async { final supabase = SupabaseClient(ApiKeys.supabaseUrl, ApiKeys.supabaseAnonKey); try { await supabase.rpc('execute_sql', params: {'sql': 'DROP TRIGGER IF EXISTS qa_notify ON public.helpline_questions; DROP TRIGGER IF EXISTS qa_fcm_notify ON public.helpline_answers;'}); print('dropped'); } catch(e) { print(e); } exit(0); }
